import React from "react";

// This root-level file is kept for compatibility but should not declare a file route.
// The real register route lives in `src/routes/register/register.tsx`.
export default function RegisterPlaceholder() {
  return null;
}
